query getMessages{
    messages {
        id
        message
        shortUrl
    }
}

mutation addMessage($message: String!) {
    message(message: $message) {
        id
        message
        shortUrl
    }
}
